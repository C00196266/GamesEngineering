#include <iostream>
#include <vector>
#include <windows.h>
#include <stdio.h>
#include <tchar.h>
#include <string>

int main(int argc, char **argv) {
	std::cout << argv[0] << std::endl;

	//for (int i = 0; i < 3; i++) {
	//	row.push_back(0.0);
	//
	//	for (int j = 0; j < 3; j++) {
	//		row.at(i) += a[j] * b[j][i];
	//	}
	//}
}